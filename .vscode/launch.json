// launch.json文件内容如下：主要是host和port
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "blog",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "remotePath": "",
            "program": "${workspaceRoot}",
            "env": {},
            "args": []
        }
    ]
}